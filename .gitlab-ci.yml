stages:
  - Build
  - Backup
  - Deploy


Build VueJS:
  stage: Build
  tags:
    - macos
  script:
    - npm install
    - npm run electron:build
  artifacts:
    paths:
      - dist_electron/
  except:
    - schedules


